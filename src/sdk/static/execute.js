'use strict';

/**
 * Execute a resource.
 *
 * @param {object}
 * @param {object}
 * @return {Promise}
 */
function execute (options, context = {}) {
  return global.Promise.resolve();
}

module.exports = execute;
