'use strict';

const should = require('should');

describe('Executor', () => {
  const Executor = require('../../../lib/agent/executor');

  describe('execute', () => {
    const { execute } = Executor;

    it('', () => {
    });
  });
});
