'use strict';

const should = require('should');

describe('ResourceParser', () => {
  const ResourceParser  = require('../../lib/resource/parser');

  describe('parse', () => {
    it('', () => {
    });
  });
});
